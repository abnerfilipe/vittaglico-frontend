version: 0.3

env:
  variables:
    FLUTTER_HOME: /opt/flutter
    AWS_ACCOUNT_ID: "535615247325"
    IMAGE_REPO_NAME: "vittaglico-frontend"
    IMAGE_TAG: "latest"
    AWS_REGION: "us-east-2"

phases:
  install:
    commands:
      - git clone https://github.com/flutter/flutter.git $FLUTTER_HOME
      - export PATH="$PATH:$FLUTTER_HOME/bin"
      - flutter channel stable
      - flutter upgrade
      - flutter doctor -v
      - flutter pub get
  build:
    commands:
      - flutter build web --release
      - echo "Construindo imagem Docker com Nginx para Flutter Web..."
      - docker build -t $IMAGE_REPO_NAME:$IMAGE_TAG -f Dockerfile .
      - docker tag $IMAGE_REPO_NAME:$IMAGE_TAG $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG
  post_build:
    commands:
      - echo "Autenticando com o ECR..."
      - aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com
      - echo "Enviando imagem para o ECR..."
      - docker push $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG
      - echo "Imagem enviada para o ECR: $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG"

artifacts:
  files:
    - '**/*'
  base-directory: 'build/web'